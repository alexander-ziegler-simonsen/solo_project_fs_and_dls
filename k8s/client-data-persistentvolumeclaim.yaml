apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: client-data
  name: client-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
