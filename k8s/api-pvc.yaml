apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: api-pvc
  namespace: ors
  labels:
    app: api-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
